name: Keep Koyeb Alive

on:
  schedule:
    # Run every 45 minutes
    - cron: "*/45 * * * *"
  workflow_dispatch: # allows manual trigger

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Ping Koyeb app
        run: |
          echo "🔄 Starting ping at $(date)"
          if curl -I https://homeless-margalo-ayushmaan-personal-109aa799.koyeb.app/; then
            echo "✅ Ping succeeded at $(date)"
          else
            echo "❌ Ping failed at $(date)"
          fi
